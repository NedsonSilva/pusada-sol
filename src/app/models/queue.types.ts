export class Queue {
    id: number;
    name: string;
    color: string;
    greetingMessage: string;
    createdAt: Date;
    updatedAt: Date;
    // whatsapps: Array<Connection & { ConnectionQueue: ConnectionQueue }>;

    // users: Array<User & { UserQueue: UserQueue }>;
  }
